services:
  - type: web
    name: autoreply
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: AUTOREPLY_API_KEY
        generateValue: true
